{
  "name": "chf-app",
  "version": "1.0.0",
  "description": "CHF - Système Hospitalier (modules CommonJS + Express)",
  "main": "server.js",
  "scripts": {
    "build": "tailwindcss -i ./styles/input.css -o ./public/tailwind.css --minify && node build.js",
    "watch": "node build.js --watch",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "tailwindcss": "^3.4.19"
  }
}
